# Caffeine

Tiny macOS menu-bar app that keeps the Mac awake using the built-in `caffeinate` command.

## Build

```sh
./build.sh
open build/Caffeine.app
```

Menu options: activate/deactivate, 15m, 30m, 1h, 2h, forever.
